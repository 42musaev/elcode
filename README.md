## The platform to store and share your code

#### Run development
```sh
docker-compose -f docker-compose.dev.yml up --build
```

#### Run production ( update .env file )
```sh
docker-compose up --build
```

